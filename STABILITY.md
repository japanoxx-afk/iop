# v0.018 안정성 점검

## v0.019 검은 화면 로그 분석

제공된 `20260914-235243-18076-report-235301/launcher.jsonl`에는 지정 게임 폴더의 `ddraw.dll`과 `ddraw.ini`가 모두 없었다. `configs`도 비어 있었다. 해당 PC는 NVIDIA RTX 4060 Laptop GPU와 Intel Iris Xe가 함께 있는 Windows 11 노트북이다. 보고 시각까지 `launch_requested`, `launch_ready`, `game_started`, PID 및 Windows 오류 이벤트가 없어 검은 화면 당시 게임 프로세스의 상태나 종료 코드는 이 자료만으로 확정할 수 없다.

v0.019는 cnc-ddraw v7.1.0.0의 `ddraw.dll`과 기본 `ddraw.ini`가 없을 때 GitHub 배포 manifest의 크기와 SHA-256을 검증해 설치한다. 이 래퍼는 구형 DirectDraw 게임의 검은 화면과 창 모드 호환을 목적으로 한다. 런처가 선택한 창/전체 화면 값은 설치된 설정의 `[iop]` 섹션에 반영한다. 기존 설정 파일은 보존한다. 진단에는 모니터/DPI 정보와 런처 밖에서 실행된 `iop.exe` 후보도 기록한다.

2026-09-14 01:10 및 01:24의 Windows 이벤트와 두 iop.exe 덤프를 분석했다. 두 건 모두 예외 0xC0000005, iop.exe+0x6F936 (가상 주소 0x46F936)에서 읽기 접근 위반이다. 버퍼 기반 주소에 더해지는 오프셋은 각각 -490, -4였다. 정상 화면 버퍼 앞을 읽고 있었다. 호출 스택의 복귀 주소는 0x481B0B로 일치한다.

해당 루틴은 효과 ID 0x07000037의 픽셀 변위/팔레트 렌더링 경로다. 화면 밖으로 밀린 샘플 좌표를 읽기 전에 제한하지 않는다. v0.014 이후 런처 변경이 이 원본 코드를 수정한 것은 아니다. 특정 맵·유닛·효과 발생 조건과 재현 빈도는 아직 확정하지 않았다.

## 충돌 우회

문제 효과 렌더링 경로의 시작 0x46F750에서 원래 성공 반환 경로 0x46F981로 이동한다. 화면 왜곡 효과 하나를 생략하며 전투 수치나 생산 명령은 변경하지 않는다. iop.exe의 10바이트만 변경하고 최초 원본을 `iop.exe.before-render-fix.bak`으로 남긴다. 알려진 EXE 해시·바이트만 지원하며 게임 실행 중 파일 변경은 Windows에 의해 차단된다. 단축키 및 서버 주소 변경에서도 이 패치를 유지한다.

효과를 생략하는 우회이지 잘못된 샘플 좌표를 근본적으로 고친 패치는 아니다. 화면 효과가 일부 달라진다. 원본 x86 오류 읽기와 우회 후 정상 복귀·스택 복원을 Unicorn으로 확인했다. 실제 장시간 대전 무충돌은 아직 확인하지 않았다. 양쪽 런처를 v0.018 이상으로 업데이트한다.

## 자동 분석 자료

런처를 켜둔 채 게임이 0이 아닌 종료 코드로 종료되면 Windows 이벤트 수집을 기다린 뒤 `diagnostics/*-exit-프로세스ID.zip`을 만든다. 종료 코드 1 등은 정상 종료와 구별되지 않을 수 있어 모든 비영 종료를 진단 대상으로 취급한다.

ZIP에는 종료 코드, 실행파일·그래픽 DLL·주요 데이터 파일 해시, 멀티맵 해시 목록, 그래픽 설정, 기존 런처 로그가 포함된다. Windows가 `%LOCALAPPDATA%\CrashDumps\iop.exe.PID.dmp`를 생성한 경우 해당 프로세스 덤프만 포함한다(128 MB 이하). 다른 프로그램의 덤프나 계정 DB는 수집하지 않는다. 덤프가 없는 경우에도 ZIP은 생성된다. 자료는 로컬 저장만 하며 자동 전송하지 않는다.

프리서버 탭의 진단 로그 폴더에서 ZIP을 찾거나, 검은 화면/오류 진단 ZIP 버튼으로 수동 수집할 수 있다. Windows 자체 덤프 설정을 이번 변경에서 시스템 전체에 강제 적용하지 않는다.
