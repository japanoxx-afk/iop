# IOP 리플레이 구현 검토

## 현재 확인 결과

2026-09-13 로컬 런처와 iop.exe 정적 분석 기준. 재생 엔진 구현이나 대전 재현 검증을 완료한 문서가 아니다.

- `iop_server/main.py`의 Lobby는 로그인, 방 생성/참가, 접속 주소 전달, 채팅을 처리한다. kind 13 결과 수신은 로그만 남긴다. 전투 명령 스트림을 저장하거나 재생하지 않는다.
- 방 참가 응답은 방장 IP와 포트를 전달한다. 따라서 런처 서버의 접속 로그만으로 전투를 재현할 수 없다.
- `iop.exe`는 DPLAYX.dll 및 WSOCK32.dll을 사용한다. 네트워크 수신 바이트만 기록하는 것도 싱글플레이의 로컬 명령·난수·AI 진행을 포괄하지 못한다.
- 실행파일 문자열 검색에서 리플레이 파일명/확장자/메뉴를 식별하지 못했다. 이는 기능 부재의 증명은 아니다.
- SAVEGAME 경로 문자열 0x4D1FD8, `.SAV` 문자열 0x4D2158, 세이브 목록 패턴 0x4D29EC와 참조 코드를 확인했다. 0x413A2E에서 0x415B10 호출 경로가 있으나 세이브 포맷 전체와 재현 가능성은 미확인이다. 일반 세이브와 명령 기반 리플레이는 동일하지 않다.
- `iop_diagnostics.launch`는 실행 프로세스를 감시하고 종료를 감지한다. 자동 파일 마무리 지점으로 사용할 수 있다. 프로세스 종료는 게임 안에서 한 판이 끝나 메뉴로 돌아오는 시점과 다르다.

## 요청한 기능에 필요한 작업

1. 경기 초기화·종료와 시뮬레이션 틱, 초기 난수 시드, 플레이어/종족/팀/맵 상태 식별.
2. 로컬 및 원격 플레이어 명령을 같은 순서로 실행하는 지점 식별. 마우스 좌표나 네트워크 패킷 도착 시간 대신 시뮬레이션 틱과 확정 명령을 기록.
3. 버전별 실행파일/밸런스/맵 해시를 포함한 IOP 전용 기록 포맷과 중단 복구 가능한 스트림 작성. 게임별 바이너리 형식이 다르므로 StarCraft .rep 파일 호환을 의미하지 않는다.
4. 기록된 초기 상태와 명령으로 네트워크 없이 동일한 시뮬레이션을 구동하는 재생 경로 구현.
5. 관전자 카메라, 입력 차단, 일시정지 및 배속 구현. 시점/시야 전환은 렌더링과 유닛 선택·공격 판정에 영향을 주지 않게 분리.
6. 정상 경기 종료 시 파일 완성, 프로세스 비정상 종료 시 미완성 기록 복구. 런처를 먼저 닫는 경우에도 기록 수명 관리.
7. 동일 기록 반복 재생 및 A/B 기록 비교에서 주기적 유닛·자원 상태 해시가 일치하는지 확인. 이동/생산/전투/AI/퇴장/속도 변경과 사용자 맵을 포함해 검증.

## 구현 범위 구분

사용자는 영상 녹화를 제외하고 게임 내부 리플레이 개발을 요청했다. v0.016은 영상 녹화를 제거한다. 재생 가능한 데이터가 없는 종료 로그/메타데이터를 리플레이라고 표시하거나 재생 버튼만 추가하지 않는다.

## 추가 확인: 명령 직렬화 및 큐

- `0x4AA640`은 UI 명령 분기다. 생산 명령 23은 `0x4AAA35`로 분기하고 유효성 검사 후 `0x49FD70`을 호출한다. 선택 대상 등의 전역 상태에 의존하므로 이 함수 호출만 기록하는 방식은 불충분하다.
- `0x49FD70`은 선택 유닛별 50바이트 명령 레코드에 생산 명령 번호 5001을 넣는다. 레코드 offset 0은 예약/연속 실행 관련 플래그, 1은 실행 예정 틱, 5는 명령 번호, 9는 플레이어, 10은 유닛 ID, 18은 생산 리소스 ID다. 플래그의 전체 의미는 미확정이다.
- `0x4A8EF0`은 명령을 모으고 `0x48E630`으로 실행 예정 틱을 채운 뒤 `0x423B00`을 호출한다.
- `0x48E630`의 로컬 경로는 `[0x6C946C]+1`을 반환한다. 네트워크 경로는 추가 동기화 지연 값을 적용한다. `0x48E90C`, `0x48E930`, `0x48E971`에서 틱 증가를 확인했다.
- `0x423E20`은 명령을 고정 111바이트 슬롯에 들어가는 패킷으로 변환한다. 헤더는 `count:u8, player:u8, tick:u32, flags:u8, command:u32`. 생산 명령은 이어서 `count`개의 u16 유닛 ID와 u32 생산 리소스 ID를 담는다. 다른 명령 종류의 payload는 별도 분기이며 아직 전체 검증하지 않았다.
- `0x43AC50(player, command)`는 플레이어별 300개 슬롯 원형 대기열에 명령을 복사한다. 직접 호출 위치는 `0x423AE7`, `0x423C25`, `0x423C7C`다. 초기화 및 경기 상태 복원 없이 이 함수에 주입하는 것만으로 재생을 완성할 수 없다.
- `0x4B9B62`/`0x4B9B6F`에 C 런타임 계열 난수 시드/생성 코드가 있지만, 이것이 게임에 사용되는 모든 난수 상태를 대표하는지는 미확인이다.

검증 도구 `tools/verify_replay_commands.py`: 실제 EXE의 `0x423E20`, `0x43AC50`, `0x423CA0`을 Unicorn에서 실행했다. 생산 유닛 ID 42/43, 플레이어 3, 틱 123, 생산 리소스 0x05000002를 인코딩하고 원형 큐에 넣은 바이트가 예상과 일치한다. 잠금 함수만 모의 처리했다. 실제 대전 재생이나 결정성 검증이 아니다.

남은 핵심 장애물은 초기 상태 전체 복원, 모든 명령 종류/원격 경로의 기록, 틱별 재생 주입, UI 명령과 관전자 입력 분리, 경기 종료 감지, 장시간 상태 해시 비교다. 실행파일 패치와 사용자용 리플레이 저장/재생 기능은 아직 적용하지 않았다.
