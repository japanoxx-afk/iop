# 통합 런처 v0.006

## v0.006 GitHub 업데이트

업데이트 확인은 이 저장소의 `main/update-manifest.json`만 읽습니다. 새 EXE는 같은 GitHub 저장소의 raw 주소만 허용하며 manifest의 파일 크기, SHA-256, Windows PE 헤더를 확인한 뒤 교체합니다. 다운로드나 검증에 실패하면 현재 런처를 유지합니다. 적용 전 기존 런처은 `.update-backup`으로 백업하고 재시작 후 결과를 표시합니다.

v0.001–v0.005의 기존 업데이트 주소 호환을 위해 `version.txt`와 `release/IOPLauncher.exe`도 함께 갱신합니다. 이후 버전은 `update-manifest.json`을 기준으로 합니다. manifest와 대상 EXE가 GitHub에 모두 게시된 경우에만 정상 업데이트됩니다.

## v0.005 유닛 생산 단축키

사용 순서: **생산 단축키 탭 → 유닛 선택 → 설정 키 선택 → 설정 저장 → 게임 시작**. 게임 폴더를 변경했다면 목록 불러오기를 누르세요. 설정은 런처의 iop_launcher_config.json에 저장되며 저장 시점에는 게임 파일을 바꾸지 않습니다. 게임 시작 전에 반영합니다. 저장하지 않은 편집이 있으면 먼저 저장하도록 안내합니다.

3종족 생산 명령 40개를 지원합니다. 영문 단일 키 A–Z 중 J/Q는 지원하지 않습니다. 숫자, Ctrl/Shift/Alt 조합키는 지원하지 않습니다. 같은 건물의 다른 유닛이나 기존 명령과 겹치면 저장을 거부합니다. 서로 다른 건물에서는 같은 키를 쓸 수 있습니다. 실제 생산 가능한 유닛·건물·자원·기술 조건은 원래 게임 규칙을 따릅니다.

기본값 복원: **전체 기본값 → 설정 저장 → 다음 게임 시작**. 개인 설정은 A와 B가 달라도 됩니다. 양쪽 v0.006으로 업데이트하세요. 오래된 런처는 수정된 실행파일을 지원하지 않거나 파일 불일치로 판단할 수 있습니다. 게임을 종료한 뒤 변경하고, 이번 세션에서 실행한 게임이 켜져 있으면 저장을 막습니다.

게임 시작 시 iop.exe 안의 생산 키 명령표와 Button.res/Kbutton.res의 생산 버튼 키 표시를 변경합니다. 수정 전 각 파일 옆에 `.before-hotkeys-숫자.bak` 백업을 만듭니다. 실행파일 이름, 네트워크 주소 설정, 화면 설정은 기존 방식으로 유지합니다. 정상적인 복원은 위 기본값 버튼을 사용하세요. 여러 시점의 EXE/리소스 백업을 임의로 혼합하지 마세요.

동기화 검사는 검증된 생산 키 변경과 생산 버튼의 키 표시만 기본값으로 정규화합니다. 다른 명령표 변경·생산 유닛 변경·밸런스 데이터 차이는 계속 차단합니다. 게임 시작 진단 로그에 적용한 키 설정과 변경 파일 수를 기록합니다.

자동 테스트 21개 및 원본 명령 조회 코드의 x86 에뮬레이션으로 검증했습니다. 실제 게임의 모든 유닛 생산과 두 PC 대전 완주는 아직 검증하지 않았습니다. 분석 근거는 HOTKEY_ANALYSIS.md를 참고하세요.

## v0.004 창모드 기본과 진단 로그

런처를 열 때마다 **창 모드**를 기본으로 선택합니다. 이전 전체화면 저장값은 적용하지 않습니다. 사용자가 이번 실행에서 직접 전체화면을 선택하는 것은 가능합니다.

검은 화면이 나오면 게임과 런처를 가능한 그대로 두고 Alt+Tab으로 런처의 서버 탭을 연 뒤 **검은 화면 / 오류 진단 ZIP 저장**을 누르세요. 생성된 ZIP을 분석 담당자에게 전달하면 됩니다. **진단 로그 폴더**에서도 찾을 수 있습니다. 로그는 런처 폴더의 `diagnostics/날짜-시간-PID`에 자동 저장되며 쓰기 권한이 없으면 `%LOCALAPPDATA%/IOPLauncher/diagnostics`를 사용합니다.

기록 항목: 런처 오류와 실행 단계, 실행파일/그래픽 DLL 해시, ddraw.ini 및 dxwnd.ini 내용, Windows/그래픽 드라이버 정보, 프로세스 PID·응답 여부·메모리·CPU·로드 모듈, 종료 코드, 최근 15분의 IOP 관련 Windows Application 오류 이벤트, 존재하는 그래픽 래퍼 로그와 게임 표준 출력. 게임이 표준 출력을 제공하지 않으면 game-output.log는 비어 있을 수 있습니다. 검은 화면 자체를 자동 판별하거나 게임 메모리 덤프를 수집하지는 않습니다. 창 응답 정상도 렌더링 정상의 증거는 아닙니다.

게임이 실행 중이면 주기적으로 프로세스 상태를 기록하고 종료 시 종료 코드를 남깁니다. 런처를 먼저 닫거나 PC가 강제 종료되면 이후 감시는 중단됩니다. ZIP에는 PC/게임 경로, 서버 IP, 모듈 경로 등이 들어가며 계정 DB·비밀번호·전체 hosts·화면 캡처는 수집하지 않습니다. 진단 자료는 자동 전송하지 않습니다.

검은 화면 원인은 아직 확인되지 않았습니다. 이 버전의 목적은 창모드 기본 적용과 재현 PC의 원인 분석 자료 확보입니다.

## 바뀐 게임 시작

게임 실행 탭의 버튼은 **게임 시작** 하나입니다. 서버 탭의 **게임 시작**도 정확히 같은 함수를 호출합니다. 기존처럼 같은 게임 폴더의 `iop.exe`를 실행하고 `[iop]`의 화면 모드만 변경합니다. renderer 등 기존 호환성 옵션은 보존합니다. 이전 `iop_private.exe`는 사용하지 않습니다.

이전 버전은 파일 이름이 달라 Windows/그래픽 래퍼의 프로그램별 설정이 다르게 적용될 가능성이 있었습니다. 이번 버전은 이 차이를 없앴습니다. B PC의 검은 화면 원인이 확정된 것은 아니며, 해당 PC에서 창 모드로 다시 확인해야 합니다.

## A PC

1. 게임과 이전 런처를 종료하고 새 `IOPLauncher_Server.exe`로 교체합니다. 기존 server_data 폴더와 설정 파일은 유지합니다.
2. 게임 폴더를 지정하고 같은 Radmin VPN 네트워크에 참가합니다.
3. 서버 탭에서 내 Radmin IP를 선택하고 **서버 ON**을 누릅니다.
4. **Radmin 방화벽 허용 (관리자)**를 다시 실행합니다. v0.003은 게임 실행파일 경로와 동기화 포트가 바뀌었습니다.
5. B에게 Radmin IP를 알려줍니다. A도 **게임 시작**을 누르면 됩니다. 서버 운영 중에는 런처를 열어 둡니다.

## B PC

1. A와 같은 Radmin 네트워크에 참가하고 새 런처를 실행합니다.
2. 서버 탭의 **A PC의 Radmin IP**에 A가 알려준 IP를 한 번 입력합니다. 이 설정은 저장됩니다.
3. **게임 시작**을 누릅니다. 필요한 경우 Windows 관리자 확인에서 허용합니다.
4. 런처가 hosts 등록 완료를 확인하고, A와 게임 파일이 같으면 기존 `iop.exe`를 실행합니다. B는 서버 ON을 누르지 않습니다.
5. 게임 연결이 방화벽에 막히면 B도 **Radmin 방화벽 허용 (관리자)**를 실행합니다.

A의 IP가 바뀌면 B의 입력란도 바꿔야 합니다. 다른 PC의 Radmin IP를 자동 검색하거나 Radmin 네트워크 가입을 자동으로 처리하지는 않습니다. 두 PC가 공유기 뒤에 있어도 VPN 연결을 이용할 수 있으며 hosts 자체는 공유기 연결이나 포트 전달 기능이 아닙니다.

## hosts 자동 등록과 실행파일 백업

입력한 A IP가 우선입니다. 없으면 기존 접속 주소 또는 hosts 매핑을 사용합니다. 네트워크 접속 설정이 전혀 없으면 로컬 게임을 실행합니다.

게임 시작은 다음 순서로 진행됩니다.

1. 로컬 화염병 수정 상태 확인 및 필요한 수정 적용.
2. `iop.server`를 A IP로 등록. 이미 맞으면 관리자 작업 생략.
3. 관리자 작업 종료 코드와 Windows 이름 해석 결과 확인. 취소/실패하면 실행 중단.
4. A 로비와 동기화 서비스 연결, A·B 파일 해시 비교.
5. 검증된 `iop.exe`에 생산 키 설정과 서버 주소를 반영하고 동일한 실행파일 이름으로 시작.

hosts는 다른 항목을 보존하며 기존 파일은 같은 폴더의 `hosts.iop-backup-숫자`로 백업합니다. 게임은 숫자 IP에 접속하므로 hosts 변경과 실행파일의 주소 반영이 함께 필요합니다.

게임 EXE 최초 변경 전 `iop.exe.before-server.bak`를 생성하고 이후 덮어쓰지 않습니다. 원상복구하려면 게임 종료 후 이 백업을 `iop.exe`로 복사합니다. 백업은 최초 변경 직전 상태이므로 이전에 주소가 패치되어 있었다면 그 상태를 보존합니다. 지원되는 EXE는 주소 필드를 원본으로 정규화한 SHA256이 `489f2217dc13e9b7b8b3fee97bd78843e70a2166f4ce50efc429c5cb1ec86028`인 버전입니다.

## 화염병과 A·B 동기화

런처 시작 시 로컬 `data/Gweapon.res`의 화염병 ID 7, 무기 타입 0x07 레코드에서 기존 피해 처리 수정 비트 `0x20000`을 확인합니다. 없으면 원본 리소스를 `Gweapon.res.before-flame-숫자.bak`로 백업한 뒤 해당 비트만 적용합니다. 서버 ON과 게임 시작 때도 다시 확인합니다. 기존의 다른 밸런스 변경이나 화염병 대체 무기 패치를 자동으로 되돌리지는 않습니다.

저장된 A IP가 있으면 런처 시작 시에도 원격 비교를 시도하고 서버 로그에 표시합니다. A가 OFF면 비교 대기 상태로 안내하며 게임 시작 때 다시 검사합니다. 게임 시작 시 불일치하면 파일 이름을 표시하고 실행을 막습니다. 양쪽 게임을 종료한 뒤 A와 같은 데이터/패치를 적용하고 다시 시작하세요. 원격 파일을 무조건 다운로드하거나 덮어쓰지는 않습니다.

비교 범위: 주소를 정규화한 `iop.exe`, `data`, `map`, `script`, `scripts` 폴더 안의 `.res`, `.map`, `.scr`, `.dll` 파일. 화면 설정, 저장 게임, 오디오/영상 등은 비교하지 않습니다. 서버는 매 요청 시 현재 파일을 읽습니다. 화염병 수정 비트뿐 아니라 다른 밸런스/맵 차이도 이 범위에서 감지합니다.

양쪽 모두 v0.003 이상이 필요합니다. 이 검사는 런처 실행 전 검사이며 구형 런처의 접속을 프로토콜 수준에서 막거나 대전 중 데이터 변경을 감시하지 않습니다. 플레이 도중에는 게임 데이터나 밸런스를 변경하지 마세요. 파일 일치는 실제 대전에서 화염병 동작이 완벽하다는 보증은 아닙니다.

## 서버와 방화벽

로비 TCP 6300/6302/6305, 읽기 전용 파일 해시 서비스 TCP 6306을 사용합니다. 게임 호스트의 IP와 포트를 로비가 참가자에게 전달합니다. 게임 기본 TCP 포트는 4801이며 실제 방장의 포트가 전달됩니다. 방화벽 버튼은 Radmin 어댑터에 한해 런처 서버 포트와 `iop.exe` TCP/UDP 수신을 허용합니다. 런처 경로를 옮기면 다시 적용하세요.

OFF/런처 종료는 해당 런처가 시작한 서버만 종료합니다. 계정은 `server_data/accounts.sqlite3`, 로그는 `server_data/server.log`에 저장됩니다. 비밀번호는 salt + PBKDF2 해시로 저장합니다. 배포 ZIP에는 게임, 계정, 개인 IP 설정을 포함하지 않습니다.

## 검증과 한계

자동 테스트 21개: 생산 키 패치/복원/충돌 및 개인 설정 동기화, 창모드 기본값과 진단 ZIP 내용 검증, 로비 등록/로그인/방/채팅, 서버 ON/OFF/재시작 및 포트 충돌 처리, hosts 보존/백업, 관리자 작업 취소 시 실행 차단, 동일 EXE 이름과 주소만 변경, 화염병 백업/멱등성/잘못된 리소스 거부, 실시간 메타데이터 비교와 변경 감지, 화면 호환성 설정 보존.

실제 시스템 hosts/방화벽 변경은 개발 테스트에서 실행하지 않았습니다. 임시 파일과 모의 관리자 응답으로 검증했습니다. 서로 다른 실제 두 PC의 검은 화면 해결 및 화염병 사용을 포함한 대전 완주는 아직 미검증입니다. 실험용 호환 로비이며 전적 저장·랭킹·공식 계정 연동은 지원하지 않습니다.

## 소스 빌드

Python 3.11 이상과 PyInstaller:

```bat
python -m unittest discover -s tests -v
pyinstaller --onefile --noconsole --name IOPLauncher_Server --distpath release --add-data "iop_balance.py;." iop_launcher.py
python package_release.py
```

기반 저장소: https://github.com/japanoxx-afk/iop
기반 HEAD: 08e2cd59ff7854e041a91b32638a6abf74bf1c0e
v0.006 소스와 실행파일은 위 GitHub 저장소의 main 브랜치에 게시합니다.
