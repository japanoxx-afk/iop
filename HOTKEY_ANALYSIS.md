# 유닛 생산 단축키 분석 및 검증

지원 실행파일: 주소 필드를 원래 값으로 정규화한 SHA256 489f2217dc13e9b7b8b3fee97bd78843e70a2166f4ce50efc429c5cb1ec86028.

- 0x4A66CE 이후 초기화 코드: DirectInput 스캔 코드별 콜백을 0x4B5CD0에 등록. 예: I=0x17 → 0x4A2FE0, K=0x25 → 0x4A3220.
- 각 알파벳 콜백은 해당 명령표 주소를 인자로 0x4AA300을 호출한다.
- 0x4AA4BC–0x4AA50A: 현재 선택 대상 타입과 13바이트 레코드의 첫 u32를 비교. 레코드 구조는 `<IBII` (선택 대상, 명령 번호, 인수 1, 인수 2). 명령 23은 유닛 생산, 인수 1은 생산 유닛 리소스 ID.
- 예: 원래 I 표의 `(0x0A000003, 23, 0x05000002, 0)`는 야전막사에서 소총병사를 생산한다.
- 0x4DB080–0x4DC410 구간의 알파벳 명령표들을 같은 5008바이트 구간에 재배치한다. 생산 레코드 40개만 키별 표 사이에서 이동한다. 비생산 레코드 313개는 해당 키에 그대로 남긴다. 각 표의 코드 참조와 A/H/P의 특수 포인터 비교 참조도 갱신한다. Escape 테이블 0x4DC410은 변경하지 않는다.
- J는 일반 명령표 경로를 사용하지 않고 Q는 해당 등록표에서 확인되지 않아 지원하지 않는다. 숫자/조합키도 지원 범위 밖이다.
- Button.res와 Kbutton.res의 생산 버튼(제품 참조 타입 0x15, 버튼 동작 종류 2) 이름 끝 `(키)`의 한 바이트만 수정한다. 이동/수리/기술 버튼에는 적용하지 않는다.
- 변경 전 EXE·리소스 백업. EXE 쓰기 핸들을 먼저 열어 실행 중인 게임에 대한 수정 실패가 리소스 수정보다 먼저 일어나도록 한다.
- A·B 비교는 임의로 명령표 전체를 무시하지 않는다. 현재 테이블이 허용된 생산 키 이동만으로 재생성되는지 검증한 후 기본값으로 정규화한다. 비생산 명령이나 생산 대상 변조는 거부한다. 버튼 파일에서는 식별된 생산 버튼 키 표시만 정규화한다.

검증: 자동 테스트 21개 통과. 지원되는 단일 유닛/키 조합의 패치·정규화·복원, 중복/지원 밖 키 거부, 리소스 변경 범위, A·B 개인 키 차이 허용 및 밸런스 불일치 감지 포함. Tk 화면의 40행 목록 선택·편집·저장·기본값 복원을 확인했다.

원본 x86 명령 조회 루틴을 Unicorn으로 실행하여 I→K 변경 후 동일 생산 명령 반환, 기존 I 비활성, 비생산 명령 313개 동일 반환을 검증했다. 별도 개발 검증 스크립트는 상위 작업 폴더의 verify_hotkey_dispatch.py. 실제 게임에서 각 유닛의 생산 및 두 PC 대전 완주는 별도 확인이 필요하다.
