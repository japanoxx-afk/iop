# 멀티 맵 에디터 (v0.010)

## 사용법

1. 게임 폴더를 지정하고 `멀티 맵 에디터`에서 기존 멀티 맵을 엽니다.
2. `타일 브러시`: 왼쪽 목록에서 타일을 고르거나 지도에서 우클릭으로 채집합니다. 좌클릭으로 적용합니다. 타일 그래픽과 원본 통행 속성을 함께 사용합니다.
3. `언덕/영역 복사`: 기존 언덕의 절벽·경사로를 포함한 첫 모서리를 우클릭하고, 반대 모서리를 Shift+우클릭합니다. 좌클릭 위치를 왼쪽 위 모서리로 붙여넣습니다. 자원과 나무/장식은 복사하지 않습니다. 경계 연결은 수동 편집입니다. 높이를 자동으로 올리는 브러시는 없습니다.
4. `자원 추가/삭제`: 좌클릭으로 배치하거나 가까운 자원을 삭제합니다. 기존 게임이 생성하는 자원 타입을 사용합니다. 자원 종류/매장량 변경은 지원하지 않습니다.
5. `선택 타일로 빈 맵`: 현재 크기와 타일 세트를 유지한 채 선택 타일로 채웁니다. 자원/나무/장식을 제거하고 시작 위치는 유지합니다.
6. `되돌리기`: 최근 30회 편집까지 취소합니다.
7. `새 맵으로 저장`: 새 이름으로 저장합니다. 원본이나 기존 파일은 덮어쓰지 않습니다. A·B의 `map/multi`에 동일 파일을 배치해야 합니다. 자동 전송 기능은 없습니다.

게임 내 이동, 경사로 연결, 건설, 자원 채취와 두 PC 대전은 별도 검증이 필요합니다. 서버/게임 실행 중 저장은 차단합니다. 기존 나무/장식은 타일 편집 후에도 남을 수 있으며 빈 맵 기능은 이를 제거합니다.

맵 뷰어는 실제 타일 그래픽을 축소해 그립니다. 청록색 마름모는 자원 생성 좌표, 노란 숫자는 시작 위치 번호입니다. 게임 화면의 건물/나무 스프라이트까지 합성하는 뷰어는 아닙니다.

## 판독 근거

- `0x412960`: 헤더와 시작 위치, 자원, 나무/장식 레코드를 읽습니다.
- `0x40654F–0x4065B9`: 자원 좌표를 32배하여 리소스 ID `0x0A200031`의 오브젝트를 생성합니다.
- `0x427370`: MAP v1/v2를 읽는 실제 지형 로더입니다.
- 헤더: u16 헤더 길이, u16 버전, u8 테마, u8 인원, 8개의 `(i16 x,i16 y)`, u16 자원 개수.
- v1: 바로 `(u16 x,u16 y)` 자원 배열.
- v2: 추가 u16 장식 개수 + 8바이트 보존 필드, 자원 배열, 8바이트 장식 배열.
- 지형: u32 폭/높이/타일 개수, x 우선 u32 타일 격자, 타일당 u16 속성 + 32×32 팔레트 인덱스(1026바이트), 폭×높이 미니맵, 16바이트 사용 영역.
- 기존 뷰어는 미니맵 뒤 사용 영역을 8바이트로 계산했습니다. 실제로는 16바이트입니다. 새 뷰어는 타일 그래픽에서 직접 렌더링합니다.
- `0x4276BE–0x42770A`: 원본 속성 비트 `0xF003`, `4`, `0x10`, `8`, `0x800`을 런타임 속성으로 변환합니다.
- `0x480A6E–0x480A8E` 및 `0x495C60`: 각 좌표의 타일 속성을 런타임 격자에 넣습니다.

검증: 로컬 멀티 맵 23개(사용자 test.map 포함)의 무수정 왕복 저장 바이트 일치. 원본 EXE 로더를 Unicorn에서 실행하여 원본 23개와 편집본 23개의 격자·픽셀·변환 속성·영역 일치. 파일/메모리 할당 함수만 모의 처리했습니다. 이는 게임 플레이 검증과 다릅니다.

## 언덕 시야 차단 가능성 검토 — 변경 없음

요청 의도는 낮은 지형의 관측 유닛만 있을 때 높은 지형의 적 유닛이 보이지 않게 만드는 것으로 해석했습니다. 아군 유닛을 숨기는 기능이나 모든 동맹 시야 공유를 없애는 기능과는 구분해야 합니다.

현재 지형 포맷에서 별도의 높이 배열은 확인되지 않았습니다. 로더의 속성 변환은 통행과 관련된 비트 필드이며 높이 값으로 단정할 수 없습니다. 설치된 멀티 맵의 원본 타일 속성은 주로 1과 8이고 일부 2입니다. 이 값만으로 모든 타일의 고도를 분류할 수 있다는 근거는 없습니다.

구현 자체를 불가능하다고 판단할 근거는 없지만, 단일 옵션 변경으로 구현 가능한 상태도 아닙니다. 추가로 (1) 고지/저지 분류 데이터 또는 맵별 높이 레이어, (2) 관측자별 시야 계산 경로, (3) 유닛 표시·선택·공격 대상 판정의 공통 가시성 처리, (4) 공중 유닛·동맹 관측·탐지 예외를 확인해야 합니다. 화면에만 숨기면 자동 공격이나 선택으로 적 정보가 드러날 수 있습니다.

고지대에 아군 관측자가 있으면 그 관측 결과를 공유할지, 절벽 너머 공격을 허용할지도 규칙으로 정해야 합니다. 엔진 패치 시 A·B에 동일한 패치/맵이 필요하며 결정적 시뮬레이션과 재현 테스트가 필요합니다. 이번 버전은 시야 관련 실행파일을 수정하지 않습니다.
